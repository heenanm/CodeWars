using System.Linq;

namespace CodeWars
{
    public class SumArrays
    {
        public static double SumArray(double[] array)
        {
            var sum = array.Sum();
            return sum;
        }
    }
}
