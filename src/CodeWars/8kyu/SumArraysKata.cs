using System.Linq;

namespace CodeWars
{
    public class SumArraysKata
    {
        public static double SumArray(double[] array)
        {
            var sum = array.Sum();
            return sum;
        }
    }
}
